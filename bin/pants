#!@php_bin@
<?php
use Pants\Runner,
    Zend\Loader\StandardAutoloader;

require_once "Zend/Loader/StandardAutoloader.php";

$autoloader = new StandardAutoloader();
$autoloader->registerNamespace("Pants")
           ->register();

$cli = new Runner();
$cli->run($_SERVER['argv']);
